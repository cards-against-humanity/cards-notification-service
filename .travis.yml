language: java

jdk:
  - oraclejdk8

script:
  - mvn clean package
  - mvn cobertura:cobertura
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker images

services:
  - docker